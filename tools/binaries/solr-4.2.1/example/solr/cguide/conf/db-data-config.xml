<dataConfig>  
    <dataSource type="JdbcDataSource"
            driver="com.microsoft.sqlserver.jdbc.SQLServerDriver"
            url="jdbc:sqlserver://localhost\SQLEXPRESS2008;databaseName=Guide;user=sa;password=el78Taxi;" 	/> 
             
    <document>  
    
    

	  <entity name="TestEntity" query="SELECT DISTINCT S.Id, S.Name as Seller, C.Name as City, Cat.Name as Category, Convert(varchar, Latitude) + ',' + Convert(varchar, Longitude) as LatLng 
FROM Seller S 
INNER JOIN SubCategory SC on SC.id =S.SubCategoryId
INNER JOIN Category Cat on Cat.ID = SC.CategoryId
INNER JOIN Location L on S.HeadLocationId = L.ID AND L.Deleted is null
INNER JOIN City C on C.ID = L.CityId
WHERE S.Deleted is null">
            <field column="Id" name="id" />
            <field column="Seller" name="seller" />  
            <field column="City" name="city" />  
            <field column="Category" name="category" /> 
	    <field column="LatLng" name="latlng" />  

			
	<!--  <entity name="TestEntity" query="SELECT Id, Term, Name, Convert(varchar, Latitude) + ',' + Convert(varchar, Longitude) as LatLng FROM tag">
            <field column="Id" name="id" />
            <field column="Term" name="term" />  
            <field column="Name" name="name" />  
			<field column="LatLng" name="latlng" />  -->
		<!--	<field column="Latitude" name="latlng_0_coordinate" />  
			<field column="Longitude" name="latlng_1_coordinate" /> 
, Latitude, Longitude 			-->
      </entity>  
    </document>  
</dataConfig> 

